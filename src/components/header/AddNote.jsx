import React from "react";

const AddNote = () => {
    return (
        <button className="header__nav--add-note">
            <i className="bx bx-plus"></i> 
            <span>Add Note</span> 
        </button>
    );
};

export default AddNote;
